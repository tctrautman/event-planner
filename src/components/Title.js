import React from 'react';

const Title = props => (
    <>
        <h2>Event Planner</h2>
        <p>
            Use this timeline to see how many "event teams"
            you need to have active at a time.
        </p>
    </>
);

export default Title;
